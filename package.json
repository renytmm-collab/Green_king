{
  "name": "green-king-lan-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal LAN two-player pointer synchronization prototype.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/server.test.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "ws": "^8.21.1"
  }
}
